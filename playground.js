const markup = `<nav class="bg-orange-500" >
    <h1 class="text-xl font-bold text-center">Hello Aniha 🚀</h1>
   </nav> 
   `;
import feather from 'feather-icons';

feather.replace(); // This will scan the DOM and replace all feather icons

